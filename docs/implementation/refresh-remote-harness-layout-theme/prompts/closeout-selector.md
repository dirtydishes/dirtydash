# Closeout Selector Prompt: Refresh, Remote Sync, Harness, Layout, And Theme

> **Superseded prompt — stop:** do not use this closeout selector to advance or close work. The `dirtydash-refresh-loop` stream is historical only and has been replaced for active remote execution by `dirtydash-px3`.

Use the active remote stream docs in `docs/implementation/remote-hub-collector-fleet/` instead. Preserve this path for history only; use git history if you need the prior prompt body.
