# Implementation Thread Prompt: Refresh, Remote Sync, Harness, Layout, And Theme

> **Superseded prompt — stop:** do not use this prompt to launch implementation work. It belongs to the obsolete `dirtydash-refresh-loop` stream and still points at stale branch/PR policy.

Use the active remote stream docs in `docs/implementation/remote-hub-collector-fleet/` instead. Preserve this path for history only; use git history if you need the prior prompt body.
